export default function HomePage() {
  // This page will be handled by middleware
  // If user has token -> redirect to /dashboard
  // If user has no token -> redirect to /auth/login
  return null;
}
